-- crée une nouvelle base de données capable de gérer n'importe quel texte, dans n'importe quelle langue, avec des recherches flexibles.
CREATE DATABASE IF NOT EXISTS social_network
CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;